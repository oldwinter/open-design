---
/*
 * This recap currently has one translated body. Generate only the Simplified
 * Chinese route so other locale prefixes never expose an English fallback as
 * if it were localized.
 */
import OsakaMeetupPage from '../../../community/open-design-osaka-meetup/index.astro';

export function getStaticPaths() {
  return [{ params: { locale: 'zh' } }];
}
---

<OsakaMeetupPage />
